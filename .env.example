PORT=8787
SYNAU_HOST=127.0.0.1
SYNAU_HMR_PORT=18787
SYNAU_CORS_ORIGIN=http://127.0.0.1:8787
SYNAU_COOKIE_SECURE=false
SYNAU_COOKIE_SAMESITE=lax
SYNAU_SESSION_TTL_DAYS=30
SYNAU_TRUST_PROXY=false
# Keep false in production. Set true only for an explicitly controlled bearer-token QA harness.
SYNAU_ALLOW_BEARER_AUTH=false
SYNAU_DEMO_MODE=false
SYNAU_STORAGE=supabase
SYNAU_PROVIDER_ID=sumopod
SYNAU_PROVIDER_NAME=Sumopod
SYNAU_OPENAI_BASE_URL=https://ai.sumopod.com/v1
SYNAU_OPENAI_MODEL=deepseek-v4-flash
SYNAU_OPENAI_API_KEY=
SYNAU_PROVIDER_TIMEOUT_MS=120000
# Lesson articles use a streamed Markdown tool call. Range: 1000-12000.
SYNAU_LESSON_MAX_OUTPUT_TOKENS=6000

# Development-only diagnostics. Keep false in production and never log tokens.
SYNAU_PERF_LOG=false
SYNAU_PERF_DEBUG=false
SYNAU_AUTH_CACHE_MS=5000

# Platform credits. Generator pricing is fixed at 1 credit per successful roadmap,
# lesson, or quiz generator. Token telemetry does not change the user charge.
SYNAU_DEMO_CREDITS=10000

# Midtrans is sandbox by default. Keep all secret keys out of source control.
SYNAU_MIDTRANS_MERCHANT_ID=
SYNAU_MIDTRANS_CLIENT_KEY=
SYNAU_MIDTRANS_SERVER_KEY=
SYNAU_MIDTRANS_PRODUCTION=false

# Google is the only supported product authentication method. Supabase is the
# only application storage and authentication backend.

# Supabase project configuration. The VITE_* aliases are used by this Vite app;
# NEXT_PUBLIC_* aliases are kept for Supabase tooling and future Next.js clients.
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=
# Set this only when the static frontend is hosted separately from Express,
# for example https://api.example.com. Leave empty for the local same-origin app.
VITE_API_BASE_URL=
# GitHub Pages sets this to /<repository-name>/ during its build. Keep / locally.
VITE_BASE_PATH=/
# Server-only. Required only when this project's Data API accepts secret keys.
SUPABASE_SECRET_KEY=

# One-time hosted Supabase Google Auth setup. SUPABASE_ACCESS_TOKEN is a
# Supabase Personal Access Token for the Management API, not sb_secret.
# Keep these values local and remove them after configuration if not needed.
SUPABASE_PROJECT_REF=
SUPABASE_ACCESS_TOKEN=
SUPABASE_GOOGLE_CLIENT_ID=
SUPABASE_GOOGLE_CLIENT_SECRET=
